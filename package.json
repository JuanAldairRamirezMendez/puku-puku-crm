{
  "name": "puku-puku-crm",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npx prisma generate && npx prisma migrate deploy && cd ../frontend && npm install && npm run build",
    "start": "cd backend && node src/server.js",
    "seed": "cd backend && node prisma/seed-churn.js"
  }
}
